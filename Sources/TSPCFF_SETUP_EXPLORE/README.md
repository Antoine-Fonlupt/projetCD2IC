These two bench performs different tests : 
- testbench.sv uses a for loop and it is up to the user to configure the windows and step accordingly to see the relevant behaviour of the cell
- testbench_gradient.sv implements Newton's gradient descent algorithm